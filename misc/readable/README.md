# Human-readable SID assignment

## ietf-datastores

Paths of data through the datastore pipeline

```
startup -> running -> intended -> operational
```

```
candidate -> running -> intended -> operational
```

```
dynamic -> operational
```

The created .sid file have SID that are increasing on each path.
It is very similar to the YANG module document order (order of statement in the .yang file).

